# Progress: plan_tdd

## 현재 Phase
Phase 1 — 구현 완료, 커밋 전 검증까지 마침

## 직전 세션에서 완료
- frontend devDep 설치: vitest, @vitest/ui, @testing-library/react, @testing-library/jest-dom, jsdom
- frontend/vitest.config.ts, vitest.setup.ts 생성 (jsdom, tests/**/*.test.{ts,tsx})
- frontend/package.json: test→vitest run, test:watch, test:e2e / test:e2e:ui / test:e2e:report
- frontend/tests/*.spec.ts → frontend/e2e/*.spec.ts (git mv 5개)
- frontend/playwright.config.ts: testDir → './e2e'
- 샘플 frontend/tests/features/blog/lib/posts.test.ts (3 테스트 통과)
- backend/jest.config.js 생성 (rootDir: src, testRegex .spec.ts, ts-jest)
- backend/package.json: test → jest --passWithNoTests (exit 0 확인)
- .claude/hooks/enforce-tdd.sh 작성 (+x) — 미러 경로 변환 + 제외 규칙
- .claude/settings.json PreToolUse Edit|Write|MultiEdit에 enforce-tdd 배선
- 훅 수동 검증 10 케이스 모두 예상대로 (통과 7 / 차단 3)
- CLAUDE.md 개발 프로세스에 훅 안내 한 줄 추가
- .github/workflows/playwright.yml: `npm run test:fe` → `npm run test:e2e -w frontend`
- CI 검증: `npm run ci -w frontend` 통과, `npm run format:check` 통과

## 다음 세션이 이어갈 것
1. 변경사항 커밋 (사용자 승인 후)
2. CLAUDE.md "활성 플랜" 섹션 제거 여부 결정 (플랜 종료 시 제거 지침)
3. 실제 TDD 사이클 첫 적용 시 훅이 의도대로 동작하는지 관찰

## 주의/블로커
- frontend에서 `@/` alias를 쓰는 테스트를 작성하려면 vite-tsconfig-paths 또는 vitest.config.ts의 resolve.alias 추가 필요 (ADR-004: 필요 시점에 추가 예정)
- 루트 `test:fe` 스크립트는 이제 Vitest를 실행함 (E2E가 아님). 과거 `test:fe`에 의존하던 워크플로우가 있다면 `test:e2e -w frontend`로 바꿔야 함 (CI는 이미 수정 완료)

## 컨텍스트 사용률
34%
