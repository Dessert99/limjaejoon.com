{
  "name": "limjaejoon-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:fe": "npm run dev -w frontend",
    "dev:be": "npm run dev -w backend",
    "storybook:fe": "npm run storybook -w frontend",
    "build:fe": "npm run build -w frontend",
    "build-storybook:fe": "npm run build-storybook -w frontend",
    "build:be": "npm run build -w backend",
    "test:fe": "npm run test -w frontend",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
