# limjaejoon.com

개인 포트폴리오 & 기술 블로그.

## 주석

- 절대 "생성"하지 않는다.
- 대신 이미 생성된 주석을 "수정"할 수는 있다.

## 테스트

- 테스트는 사용자가 요구할 때만 실행한다.


## 디자인 토큰

- Tailwind가 이미 주는 값을 이름만 바꿔 다시 정의하지 않는다.
- 한 view만 쓰는 토큰은 그 view의 css에 둔다. 값이 겹쳐도 각자 갖는다.

## 클래스 조합

- `cn`은 shadcn ui 컴포넌트에서만 쓴다. 복사해온 라이브러리 코드를 호출자가 덮어쓰라고 뚫어둔 통로다.
- 그 밖의 컴포넌트는 `className` prop을 열지 않는다. 여백·위치·크기는 부모가 래퍼로 준다.
- 조건부 클래스는 삼항이나 `clsx`로 붙인다. `cn`을 부르지 않는다.
- 단, 같은 엘리먼트에 놓여야만 하는 클래스(flex 컨테이너 속성·overflow·sticky)는 래퍼로 뺄 수 없다. 이때만 `className`을 연다.

## 모션

- GSAP 값(duration·ease 등)은 트윈 호출부에 인라인으로 쓴다. 상수로 빼지 않는다.
- 값이 파일 안에서든 파일 간에서든 중복돼도 그대로 둔다. 중앙 프리셋 파일로 모으지 않는다.
- 한 타임라인의 여러 트윈이 같은 값을 쓸 때만 `timeline({ defaults })`로 넘긴다.

## 개발 명령어

```sh
npm run dev        # 개발 서버
npm run build      # 프로덕션 빌드
npm run lint       # 린트
npm run ci         # lint + type-check + test + build
npm run format     # 전체 포맷
```

## 코딩 지침

- 문제를 푸는 최소한의 코드. 200줄을 썼는데 50줄로 되면 다시 써라.
- 꼭 필요한 것만 건드린다. 고장 나지 않은 코드를 리팩터링하거나 인접한 코드·주석·포맷을 "개선"하지 않는다.
- 네 변경 때문에 안 쓰이게 된 import·변수·함수는 지운다. 원래 있던 죽은 코드는 언급만 하고 지우지 않는다.
- 바뀐 모든 줄은 요청으로 곧장 추적될 수 있어야 한다.
