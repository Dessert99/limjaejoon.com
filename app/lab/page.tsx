import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '랩',
  description: '제품이 되기 전의 실험과 만들다 만 것들',
};

/** Lab — 실험을 늘어놓는 자리. 지금은 비어 있다 */
export default function LabPage() {
  return <main className='min-h-svh' />;
}
