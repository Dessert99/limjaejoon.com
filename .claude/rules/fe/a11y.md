---
paths:
    - "frontend/**/*.tsx"
---

# 접근성(a11y) 규칙

- 인터랙티브 요소(버튼, 링크)에 `:focus-visible` 스타일 필수
- 아이콘 전용 버튼은 `aria-label` 필수
- 장식용 아이콘은 `aria-hidden="true"` 추가
- 애니메이션이 있는 스타일에 `prefers-reduced-motion` 대응 필수
- `div` 클릭 금지 — 인터랙션은 `button`, `a` 등 시맨틱 요소 사용
