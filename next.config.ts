// Next.js 설정 객체 타입을 가져옵니다.
import type { NextConfig } from 'next';

// 현재 프로젝트에서 사용할 Next.js 동작 옵션을 정의합니다.
const nextConfig: NextConfig = {
  // 필요할 때 여기서 옵션을 확장합니다.
};

// 정의한 설정 객체를 Next.js 런타임에 전달합니다.
export default nextConfig;
